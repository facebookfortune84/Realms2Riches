// Prefer `src/lib/apiBase.js` (`getApiBase` / `getWsBase`) in components; this file is legacy defaults only.
const config = {
  API_URL: "https://api.realms2riches.com",
  VERSION: "11.0.0-PLATINUM",
  SOVEREIGN_MODE: true
};

export default config;
